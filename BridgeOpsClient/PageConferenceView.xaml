<Page x:Class="BridgeOpsClient.PageConferenceView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BridgeOpsClient"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageConferenceView" FrameworkElement.SizeChanged="WindowResized">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ToolBarTray Grid.Row="0">
            <ToolBar>
                <Button Content="-" x:Name="btnTimeZoomOut" Click="btnTimeZoomOut_Click" />
                <Button Content="+" x:Name="btnTimeZoomIn" Click="btnTimeZoomIn_Click" />
                <Button Content="-" x:Name="btnResourceZoomOut" Click="btnResourceZoomOut_Click" />
                <Button Content="+" x:Name="btnResourceZoomIn" Click="btnResourceZoomIn_Click" />
            </ToolBar>
        </ToolBarTray>
        <Grid Grid.Row="1">
            <local:ScheduleView x:Name="schView" />
        </Grid>
    </Grid>
</Page>
