<UserControl x:Class="BridgeOpsClient.CustomControls.NumberEntry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BridgeOpsClient.CustomControls"
             mc:Ignorable="d" 
             Height="24" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" x:Name="btnIncrement" Content="▲" Padding="0 -1 0 0" Click="btnIncrement_Click"
                FontSize="8"/>
        <Button Grid.Row="1" x:Name="btnDecrement" Content="▼" Padding="0 -1 0 0" Click="btnDecrement_Click"
                FontSize="8"/>
        <TextBox x:Name ="txtNumber" TextChanged="txtNumber_TextChanged" Grid.RowSpan="2" Grid.Column="1"
                 PreviewTextInput="txtNumber_PreviewTextInput"/>
    </Grid>

</UserControl>
